# LV_DCP v0.7.0 — Symbol Timeline Index

**Date:** 2026-04-23
**Status:** Released
**Spec:** [specs/010-feature-timeline-index/spec.md](../../specs/010-feature-timeline-index/spec.md)
**Scope:** append-only symbol history layer + indexed lookups that replace
repeated `git log` walks in Claude Code / MCP retrieval.

## Summary

`v0.7.0` ships the Symbol Timeline Index (spec-010). An append-only SQLite
event store records every symbol transition (added / modified / removed /
renamed) during scans, keyed on `(project_root, symbol_id, commit_sha)`.
Four new MCP tools expose the store through indexed lookups, giving the
assistant first-class answers to temporal questions like *"когда был
реализован X"*, *"что пропало после v0.6.0"*, *"что изменилось между
v1.0 и v2.0"*, and *"какие публичные символы исчезли недавно"* — without
walking git history blind.

## Shipped

### Data plane

- **Append-only SQLite store** at `~/.lvdcp/symbol_timeline.db`
  (override via `LVDCP_TIMELINE_DB`). WAL-mode, schema-versioned, single
  writer per project. Retention optional via `timeline.retention_days`.
- **Differ + rename detector** (`libs/symbol_timeline/differ.py`,
  `rename_detect.py`) — content-hash + qualified-name similarity scoring,
  threshold tunable via `timeline.rename_similarity_threshold` (default
  `0.85`).
- **Scanner wiring** (`libs/scanning/scanner.py`): lazy
  `_maybe_build_default_timeline_sink` reads `timeline.enabled`; zero
  overhead when the flag is off.
- **Release snapshots** (`libs/symbol_timeline/snapshot.py`): tied to
  `git tag`, idempotent insert, checksum-addressed. Tag watcher
  (`libs/gitintel/tag_watcher.py`) promotes new tags into snapshots
  in-process.
- **Reconcile loop** (`libs/symbol_timeline/reconcile.py`): marks events
  orphaned after rebase / `git commit --amend`, surfaced through
  `ctx timeline reconcile`.

### MCP tools

- `lvdcp_when(path, symbol)` — full event history + rename lineage for a
  single symbol. Fuzzy-match candidate list when the exact symbol is not
  found.
- `lvdcp_removed_since(path, since)` — symbols removed after a git
  reference (tag, SHA, branch, `HEAD~N`). Importance-weighted ranking.
- `lvdcp_diff(path, from_ref, to_ref)` — structured diff of
  added / removed / modified / renamed symbols between two refs,
  bucketed with per-bucket `limit`.
- `lvdcp_regressions(path, since, limit)` — removed-since restricted to
  symbols with high importance + recent activity, for "what broke?"
  triage.

### CLI

- `ctx timeline enable|disable|status|reconcile|prune|backfill` — thin
  Typer wrapper over library functions.

### Claude Code integration

- **Hooks** in `.claude/hooks/` for `PostCommit`, `PostCheckout`, and
  `PostRebase` — auto-run reconcile so orphaned events are cleaned up
  without manual intervention.

### Pack enrichment (Layer 4)

- `detect_timeline_markers(query)` — EN + RU stem-based regex covering
  *when was*, *since v*, *removed / удал\*/пропа[лд]\**, *history of*,
  *diff between*, *renamed from*.
- `enrich_pack_with_timeline(pack, project_root, markers, enabled)` adds
  a `## Timeline facts` section to `ctx pack` output, bounded at **3 KB**
  so enrichment never displaces retrieval results. Silent-skip on
  unreachable store.

### Observability

- **5 Prometheus metrics** (`libs/telemetry/timeline_metrics.py`):
  `symbol_timeline_events_total`, `_query_latency_seconds`,
  `_snapshot_build_seconds`, `_reconcile_orphaned_total`,
  `_sink_errors_total`.
- **structlog enrichment** — every timeline-relevant log record carries
  `event_type`, `symbol_id`, `commit_sha`, `duration_ms`.
- **Doctor check** `ctx doctor` → "timeline store": PASS / WARN / FAIL
  with 7-day freshness threshold (spec FR-008).

### Rollout + perf

- **Runbook** at [docs/runbooks/timeline-rollout.md](../runbooks/timeline-rollout.md)
  with 3-step smoke test and soft/hard rollback.
- **SC-003 perf gate** (`tests/perf/test_scan_with_timeline.py`,
  `@pytest.mark.slow`): min-of-3 wall-clock comparison; current overhead
  well under the 10 % budget.

## Success criteria — empirical results

| # | Criterion | Target | Result |
|---|---|---|---|
| SC-001 | Token footprint for "when was X" query | ≤ 5 KB (≥ 15× savings vs git log walk) | **31×–88×** savings; `lvdcp_when` responses 506–826 B vs 20 KB–44 KB for `git log -p --follow` on the corresponding file |
| SC-003 | Scan overhead with timeline vs without | ≤ 10 % | Passes with 10 ms floor to dampen CI jitter |

### SC-001 measurement (LV_DCP monorepo, commit 9f6e475)

```
libs.scanning.scanner.scan_project              → 506 B   (vs 44 627 B git log)  = 88×
libs.context_pack.builder.build_navigate_pack   → 826 B   (vs 26 048 B git log)  = 31×
libs.symbol_timeline.store.SymbolTimelineStore
  .__init__                                     → 558 B   (vs 20 771 B git log)  = 37×
```

All three responses fit well inside the 5 KB SC-001 budget; savings are
≥ 2× the 15× target on every probe.

## Validated baseline

- `uv run ruff check .` — clean across 360 files
- `uv run ruff format --check .` — 360 files already formatted
- `uv run mypy .` — `Success: no issues found in 360 source files`
- `uv run pytest -q -m "not eval and not llm"` — **1049 passed**, 1
  pre-existing failure (`test_mcp_handshake` — expired Qdrant dev SSL
  cert, environmental; unrelated to spec-010)
- `uv run pytest -m slow tests/perf` — SC-003 perf gate passes

## Rollout

Default in `TimelineConfig.enabled`: **True** for dev. Operators who want
to hold the feature until they have run a local smoke pass can set
`timeline.enabled: false` in `~/.lvdcp/config.yaml`; re-enable by
flipping back and re-scanning. Store data is append-only and survives
the flag, so soft rollback is free. See the runbook for the 3-step
validation flow.

## Known gaps

- `test_mcp_handshake` still fails in local runs because the dev Qdrant
  instance is serving an expired TLS cert — environmental, independent
  of spec-010. Will be fixed in a follow-up stabilization bump by either
  renewing the cert or wiring `verify=False` for the local smoke test.
- Timeline is populated **prospectively**: events land during scans
  performed with `enabled=true`. There is no backfill from pre-v0.7.0
  history — use `ctx timeline backfill` if you want to seed a snapshot
  from an existing `git log`. This is by design per the spec (cost
  envelope).

## Upgrade notes

1. `uv sync` to pull the v0.7.0 package.
2. Run `ctx scan <project>` once; the timeline sink will self-initialize
   at `~/.lvdcp/symbol_timeline.db`.
3. Verify with `ctx timeline status` and `ctx doctor`.
4. Optional: register the Claude Code hooks from `.claude/hooks/` so
   rebases stay reconciled automatically.

## Next up

- Phase 9 (Project Copilot Wrapper) — plan already staged in
  `docs/superpowers/plans/2026-04-13-phase-9-project-copilot-wrapper.md`.
- Environmental: renew / replace the expired Qdrant dev cert so the
  handshake test is green again.
