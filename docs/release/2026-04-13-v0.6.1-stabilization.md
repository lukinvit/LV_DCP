# LV_DCP v0.6.1 — Stabilization Release

**Date:** 2026-04-13
**Status:** Released
**Scope:** quality gates, repository health, runtime hardening

## Summary

`0.6.1` does not expand the feature surface. It stabilizes the Phase 6 release
by making the declared engineering contract enforceable and warning-free.

## Shipped

- Added mandatory GitHub Actions quality gates for `ruff`, `mypy`, non-eval
  tests, and eval tests.
- Restored repository-wide green `ruff` and green `mypy`.
- Replaced silent degraded MCP paths with explicit logging.
- Hardened embeddings execution under active event loops.
- Removed noisy local-test warnings from Qdrant compatibility checks and local
  payload index setup.

## Validated baseline

- `uv run ruff check .`
- `uv run mypy .`
- `uv run pytest -q -m "not eval and not llm"` → `651 passed, 2 deselected`
- `uv run pytest -q -m eval` → `2 passed, 651 deselected`

## Current status

- Retrieval synthetic metrics remain green at `active_phase: 4`.
- Canonical public version markers now read `0.6.1`.
- Multi-project and polyglot eval remain advisory and environment-dependent.
